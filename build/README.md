Build Scripts

Windows
- `build\windows\build.ps1` builds a one-file Windows executable for the TUI.
- Output: `dist\pyrigol-tui.exe`

Requirements
- Python 3.12
- PyInstaller (install with `uv sync --group build`)
